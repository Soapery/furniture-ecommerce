<%= form_tag(search_products_path, method: "get") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search by product name" %>
  <%= select_tag :pattern, options_for_select(ProductPattern.pluck(:pattern_name).uniq, params[:pattern]), include_blank: true %>
  <%= submit_tag "Search" %>
<% end %>